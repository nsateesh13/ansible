- name: Play level variables
  hosts: all
  vars:
    URL1: test.example.com
  tasks:
    - name: Print URL
      ansible.builtin.debug:
        msg: URL1 ={{URL1}}

- name: Play level variable from a file
  hosts: all
  vars_files:
    - sample-vars.yml
  tasks:
    - name: Print URL
      ansible.builtin.debug:
        msg: URL ={{URL}}

- name: Task level variable
  hosts: all
  tasks:
    - name: Print URL
      ansbile.builtin.debug:
        msg: URL ={{URL}}
      vars:
        URL: tasksleve.com.sg